<svg width="100%" height="100%" viewBox="0 0 800 450" xmlns="http://www.w3.org/2000/svg">
    <!-- Black background -->
    <rect width="100%" height="100%" fill="#000000"/>

    <!-- Camera icon -->
    <g transform="translate(350, 175)" fill="#ffffff" opacity="0.5">
        <path d="M50,0 L70,20 L100,20 A10,10 0 0 1 110,30 L110,80 A10,10 0 0 1 100,90 L0,90 A10,10 0 0 1 -10,80 L-10,30 A10,10 0 0 1 0,20 L30,20 L50,0 Z M50,30 A25,25 0 1 0 50,80 A25,25 0 1 0 50,30 Z"/>
    </g>

    <!-- Text placeholder - will be replaced by JavaScript if text parameter is provided -->
    <text id="placeholderText" x="400" y="300" font-family="Arial, sans-serif" font-size="24" text-anchor="middle"
          fill="#ffffff">Imagem não disponível
    </text>

    <!-- JavaScript to handle text parameter -->
    <script type="text/javascript">
        <![CDATA[
      function getParameterByName(name) {
        var url = window.location.href;
        name = name.replace(/[\[\]]/g, '\\$&');
        var regex = new RegExp('[?&]' + name + '(=([^&#]*)|&|#|$)'),
            results = regex.exec(url);
        if (!results) return null;
        if (!results[2]) return '';
        return decodeURIComponent(results[2].replace(/\+/g, ' '));
      }

      window.onload = function() {
        var text = getParameterByName('text');
        if (text) {
          document.getElementById('placeholderText').textContent = text;
        }
      }
    ]]>
    </script>
</svg>
